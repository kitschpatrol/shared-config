{
	"extends": "@kitschpatrol/typescript-config",

	// Includes project dit-files files for typescript-eslint integration
	"include": ["**/*", "**/**.*"],
	"exclude": [
		"**/dist/",
		"**/bin/",
		"test/fixtures/input/*.*",
		"test/fixtures/output-fixed-auto/*.*"
	]
}
