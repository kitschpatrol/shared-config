import type { Rules } from '../types'

export const dependRecommendedRules: Rules = {
	// Begin expansion 'eslint-plugin-depend' 'recommended'
	'depend/ban-dependencies': 'error',
	// End expansion
}
